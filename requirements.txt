mcp==2.2.0
playwright==1.63.0
yt-dlp==2026.8.19
yt-dlp-ejs==0.8.0
faster-whisper==1.2.1
imageio-ffmpeg==0.6.0

# Explicitly pin transitive packages with known fixes surfaced by Docker Scout.
msgpack==1.2.1
setuptools==78.1.1

# TikTok browser-impersonation dependencies are installed separately in the Docker image,
# wheel-only and hash-locked from requirements_tiktok_impersonation.lock.txt.
